# Optional: bake a Gemini API key into the build.
# You can also paste your key directly in the app UI (stored only in your browser).
# Get a key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=MY_GEMINI_API_KEY
